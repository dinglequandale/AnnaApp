<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.AnnaApp" parent="Theme.MaterialComponents.Light">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/grey</item>
        <item name="colorPrimaryVariant">@color/purple_600</item>
        <item name="colorOnPrimary">@color/black</item>
        Secondary brand color. -->
        <item name="colorSecondary">@color/purple_800</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>
        Customize your theme here. -->
        <item name ="android:textColor">@color/black</item>
    </style>

    <style name="Theme.AnnaApp.NoActionBar">
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.AnnaApp.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.AnnaApp.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <!-- Base application theme. -->
    <style name="Base.Theme.AnnaApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="AccessDialogTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:background">@color/grey</item>
        <item name="android:dialogCornerRadius" tools:targetApi="p">30dp</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.5</item>
<!--        <item name="android:windowBackground">@android:color/transparent</item>-->
    </style>
</resources>